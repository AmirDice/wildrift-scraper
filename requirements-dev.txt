# Exactly what the test suite imports, transitively -- nothing more.
#
# Deliberately NOT "-r requirements.txt": the tests read committed JSON, so
# pulling in the whole scrape stack would slow every CI run for nothing.
#
# If a new test imports something else, CI fails on ImportError. Add it here.
pytest>=8.0.0

# web/advisor and the LLM scripts import these at module scope, so collecting
# the tests imports them even though no test makes a network call.
requests>=2.31.0
google-genai>=1.0.0

# The scraper-pipeline tests (extraction guard, navigator, rank digits, icon
# geometry) import src modules whose module scope needs these three. HEADLESS
# opencv on purpose: CI has no display, and the full package needs libGL.
# Locally requirements-scrape.txt installs the full opencv-python; if you
# install this file afterwards and the coordinate-mapper GUI stops opening
# windows, reinstall opencv-python to put the GUI build back.
numpy>=1.26.0
opencv-python-headless>=4.9.0
pytesseract>=0.3.10
