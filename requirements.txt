# Installed into the Vercel Python serverless function (api/advisor.py), so
# this file is deliberately tiny. Everything listed here is unzipped into a
# bundle that has a 250MB ceiling, and streamlit + pandas + opencv + numpy
# together would very likely breach it on their own.
#
# api/advisor.py -> web/build_advisor.py -> web/advisor/* imports exactly one
# third-party package, verified by walking the import graph.
requests>=2.31.0

# Everything else the repo needs -- scrapers, OCR, the legacy Streamlit app --
# is in requirements-scrape.txt:
#
#     pip install -r requirements-scrape.txt
#
# and the test suite's deps are in requirements-dev.txt.
