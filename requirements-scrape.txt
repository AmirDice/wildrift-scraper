# Scrapers, OCR, and the local pipeline.
#
# Deliberately NOT in requirements.txt: that file is what Vercel installs into
# the advisor serverless function, and none of these run there. Keeping them
# out is what keeps that bundle under its 250MB limit.
#
#     pip install -r requirements-scrape.txt
-r requirements.txt

# scraping
beautifulsoup4>=4.12.0

# data_loader and the analysis pipeline
pandas>=2.2.0

# item-icon OCR and image work
pillow>=10.0.0
opencv-python>=4.9.0
numpy>=1.26.0
pytesseract>=0.3.10

# Gemini, used by the CN scraper and the item enrichment scripts
google-genai>=1.0.0
