// Feature flags.
//
// BUILD_TOOLS_LIVE gates the Build Optimizer (/build) and Counter Builder
// (/counter). They are finished but held back from production for a separate
// launch. Flip this to `true` to reveal them everywhere at once: nav links,
// landing-page flagship cards, the top feature banner, the footer CTA, the
// per-champion recommended build, the sitemap and the pages themselves.
export const BUILD_TOOLS_LIVE = false;
